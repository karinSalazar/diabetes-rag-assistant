# ============================================================
# REQUIREMENTS — Chatbot RAG Sanitario Diabetes (TFM)
# Python 3.11
# Version SIN conflictos: fastapi 0.110 no arrastra fastapi-cli,
# por lo que typer queda libre para chromadb + spacy.
# ============================================================

# -- API REST --
# fastapi 0.110.x NO incluye fastapi-cli (el causante del conflicto).
fastapi==0.110.3
uvicorn[standard]==0.29.0
pydantic==2.7.0
python-multipart==0.0.9

# -- LLM y embeddings (local) --
ollama==0.2.0

# -- Base de datos vectorial --
chromadb==0.5.0

# -- Re-ranking --
sentence-transformers==3.0.0

# -- Procesado de documentos (multiformato) --
pypdf==4.2.0
pandas==2.2.2
openpyxl==3.1.2
beautifulsoup4==4.12.3
lxml==5.2.2

# -- Chunking --
langchain==0.2.0
langchain-text-splitters==0.2.0
tiktoken==0.7.0

# -- Anonimizacion de PII --
presidio-analyzer==2.2.354
presidio-anonymizer==2.2.354
spacy==3.7.4

# -- Cloud (corpus publico) --
boto3==1.34.0
databricks-sdk==0.27.0

# -- Trazabilidad / MLOps --
mlflow==2.13.0

# -- Interfaz web --
streamlit==1.35.0
requests==2.32.0

# -- Seguridad --
passlib==1.7.4

# -- Configuracion --
python-dotenv==1.0.1

# -- Tests --
pytest==8.2.0
pytest-asyncio==0.23.0
pytest-cov==5.0.0
httpx==0.27.0

# -- Evaluacion y graficas --
matplotlib==3.9.0
seaborn==0.13.2
tabulate==0.9.0
