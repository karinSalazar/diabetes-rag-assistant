# ============================================================
# REQUIREMENTS — Chatbot RAG Sanitario Diabetes (TFM)
# Python 3.11
# ============================================================

# ── API REST ────────────────────────────────────────────────
# FastAPI: framework de la API. uvicorn: servidor que la ejecuta.
# pydantic: validación automática de los datos de entrada/salida.
fastapi==0.111.0
uvicorn==0.29.0
pydantic==2.7.0
python-multipart==0.0.9        # para subir archivos (multipart/form-data)

# ── LLM y embeddings (local) ────────────────────────────────
# Cliente Python para hablar con Ollama (Llama 3.2 + nomic-embed-text)
ollama==0.2.0

# ── Base de datos vectorial ─────────────────────────────────
# ChromaDB: almacena los embeddings del corpus y hace búsqueda semántica
chromadb==0.5.0

# ── Re-ranking ──────────────────────────────────────────────
# CrossEncoder para reordenar los fragmentos por relevancia real
sentence-transformers==3.0.0

# ── Procesado de documentos (multiformato) ──────────────────
# Cada librería lee un formato del corpus clínico
pypdf==4.2.0                   # PDFs (guías NICE, WHO, SED)
pandas==2.2.2                  # CSVs y Excel (datasets UCI, Kaggle)
openpyxl==3.1.2                # Excel .xlsx
beautifulsoup4==4.12.3         # HTML (fact sheets web)
lxml==5.2.2                    # parser rápido para BeautifulSoup

# ── Chunking ────────────────────────────────────────────────
# LangChain solo para el divisor de texto (RecursiveCharacterTextSplitter)
langchain==0.2.0
langchain-text-splitters==0.2.0
tiktoken==0.7.0                # conteo de tokens

# ── Anonimización de PII (TU SELLO DE PRIVACIDAD) ───────────
# Presidio: detección y anonimización de datos personales (Microsoft)
# spaCy: motor de NER (reconocimiento de nombres, lugares)
presidio-analyzer==2.2.354
presidio-anonymizer==2.2.354
spacy==3.7.4

# ── Cloud (corpus público) ──────────────────────────────────
# boto3: cliente de AWS S3. databricks-sdk: lanzar Jobs de Databricks.
boto3==1.34.0
databricks-sdk==0.27.0

# ── Trazabilidad / MLOps ────────────────────────────────────
# MLflow: registra cada inferencia (parámetros, métricas, artefactos)
mlflow==2.13.0

# ── Interfaz web ────────────────────────────────────────────
# Streamlit: la interfaz de usuario. requests: para llamar a la API.
streamlit==1.35.0
requests==2.32.0

# ── Seguridad ───────────────────────────────────────────────
# Para el hashing de API keys (autenticación RBAC)
passlib==1.7.4

# ── Configuración ───────────────────────────────────────────
# Carga las variables del archivo .env
python-dotenv==1.0.1

# ── Tests ───────────────────────────────────────────────────
# pytest: framework de pruebas. httpx: cliente para testear la API.
pytest==8.2.0
pytest-asyncio==0.23.0
pytest-cov==5.0.0              # cobertura de tests
httpx==0.27.0

# ── Evaluación y gráficas (Fase 9) ──────────────────────────
# Para generar las gráficas del TFM (RAGAS, ablación, red-team)
matplotlib==3.9.0
seaborn==0.13.2
tabulate==0.9.0                # tablas en formato texto/markdown