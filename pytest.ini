[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integracion: tests que requieren Ollama corriendo (lentos)
    unidad: tests rápidos sin dependencias externas

filterwarnings =
    ignore::DeprecationWarning